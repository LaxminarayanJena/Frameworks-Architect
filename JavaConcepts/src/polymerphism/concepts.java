package polymerphism;

public class concepts {

	/*
	 * same name + multiple form(action/behaviour)
	 * 
	 * type of poly:
	 * 1)compiletinme/earlybinding/static binding/overloading
	 * 2)runtime/latebinding/dynamic binding/overrding
	 * 
	 * can we achieve overloadding by keeping the method signature same and changing the return type
	 * no
	 */
}
