package com.ddf.testcases;

import org.openqa.selenium.Alert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.Select;
import org.testng.Assert;
import org.testng.annotations.DataProvider;
import org.testng.annotations.Test;

import com.ddf.base.TestBase;
import com.ddf.utilities.TestUtil;

public class OpenAccountTest extends TestBase {
	
	@Test(dataProviderClass=TestUtil.class,dataProvider="dp")
	public void OpenAccount(String customer,String currency) throws InterruptedException
	{
		                                        
		click("OpenAccountTest");
	/*	
		WebElement dropdowncustomer = driver.findElement(By.xpath(".//*[@id='userSelect']"));
		Select select1 = new Select(dropdowncustomer);		
		select1.selectByVisibleText(customer);
		
		WebElement dropdowncuurency = driver.findElement(By.xpath(".//*[@id='currency']"));
		Select select = new Select(dropdowncuurency);		
		select.selectByVisibleText(currency);
		Thread.sleep(3000);
	*/	
		//select("customerName",customer);
		//select("currencyname",currency);
		click("process");
	
	}

	
}